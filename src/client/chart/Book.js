import React from 'react';

//Get a list of chapters for each book
//Shrink and display for chapters

const Book = (props) => {
    return (
        <div>
            {props.name}
        </div>
    )
}

export default Book;